<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand me-5" href="/">Pundit</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse ms-5" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= new_post_path %>">Create Post</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Features</a>
        </li>
      </ul>

      <div class="float-end">
        <% if user_signed_in? %>
          <%= link_to 'Edit registration', edit_user_registration_path, class: "btn btn-sm btn-primary" %>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn btn-sm btn-danger" %>
        <% else %>

          <%= link_to 'Register', new_user_registration_path, class: "btn btn-sm btn-success"  %>
          <%= link_to 'Login', new_user_session_path, class: "btn btn-sm btn-primary"  %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
